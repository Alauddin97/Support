import React from 'react'

function Connect() {
    return (
        <div className='details'>
            <h3>I have 2 different accounts with 2 different brokers, can I connect both in the Same bridge?</h3>
            ​You can connect only one broker at a time, so incase you need to use two brokers you can shuffle between them by changing broker or buy another subscription for one more bridge by signing up again. 
        </div>
    )
}

export default Connect
